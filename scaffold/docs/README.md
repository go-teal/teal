# hello-world

> Generated with [Teal](https://github.com/go-teal/teal) - A Go-based ETL framework for building data pipelines

## Project Information

- **Name**: hello-world
- **Module**: github.com/you_git_user/your_project
- **Version**: 1.0.0

## Overview

This project is built using Teal, a Go-based ETL tool that generates code from SQL models, creating data pipelines with DAG-based execution.

For more information about Teal, see the [Teal README](https://github.com/go-teal/teal/blob/main/README.md).

## Database Connections

The project uses the following database connections:

### default
- **Type**: duckdb
- **Path**: ./store/test.duckdb

## Data Models

The project contains data models organized by stages:

### staging Stage

- **staging.stg_airports** (table)
  - Connection: `default`
  - Used by: , `dds.dim_airports`

- **staging.stg_crew_assignments** (table)
  - Connection: `default`
  - Used by: , `dds.fact_crew_assignments`

- **staging.stg_employees** (table)
  - Connection: `default`
  - Used by: , `dds.dim_employees`

- **staging.stg_flights** (table)
  - Connection: `default`
  - Used by: , `dds.fact_flights`

- **staging.stg_routes** (table)
  - Connection: `default`
  - Used by: , `dds.dim_routes`

### dds Stage

- **dds.dim_airports** (table)
  - Connection: `default`
  - Depends on: , `staging.stg_airports`
  - Used by: , `mart.mart_airport_statistics`, `mart.mart_flight_performance`
  - Tests: 1 test(s)

- **dds.dim_employees** (table)
  - Connection: `default`
  - Depends on: , `staging.stg_employees`
  - Used by: , `dds.fact_crew_assignments`, `mart.mart_airport_statistics`, `mart.mart_crew_utilization`
  - Tests: 1 test(s)

- **dds.dim_routes** (table)
  - Connection: `default`
  - Depends on: , `staging.stg_routes`
  - Used by: , `dds.fact_crew_assignments`, `dds.fact_flights`, `mart.mart_airport_statistics`, `mart.mart_crew_utilization`, `mart.mart_flight_performance`
  - Tests: 1 test(s)

- **dds.fact_crew_assignments** (table)
  - Connection: `default`
  - Depends on: , `staging.stg_crew_assignments`, `dds.fact_flights`, `dds.dim_employees`, `dds.dim_routes`
  - Used by: , `mart.mart_crew_utilization`

- **dds.fact_flights** (incremental)
  - Connection: `default`
  - Depends on: , `staging.stg_flights`, `dds.dim_routes`
  - Used by: , `dds.fact_crew_assignments`, `mart.mart_airport_statistics`, `mart.mart_crew_utilization`, `mart.mart_flight_performance`

### mart Stage

- **mart.mart_airport_statistics** (view)
  - Connection: `default`
  - Depends on: , `dds.fact_flights`, `dds.dim_employees`, `dds.dim_routes`, `dds.dim_airports`

- **mart.mart_crew_utilization** (view)
  - Connection: `default`
  - Depends on: , `dds.fact_crew_assignments`, `dds.dim_employees`, `dds.fact_flights`, `dds.dim_routes`

- **mart.mart_flight_performance** (view)
  - Connection: `default`
  - Depends on: , `dds.fact_flights`, `dds.dim_routes`, `dds.dim_airports`

## Raw Assets (Go Functions)

No raw assets defined in this project.

## Model Tree

```

staging/
├── staging.stg_airports [default]
├── staging.stg_crew_assignments [default]
├── staging.stg_employees [default]
├── staging.stg_flights [default]
├── staging.stg_routes [default]

dds/
├── dds.dim_airports [default]
├── dds.dim_employees [default]
├── dds.dim_routes [default]
├── dds.fact_crew_assignments [default]
├── dds.fact_flights [default]

mart/
├── mart.mart_airport_statistics [default]
├── mart.mart_crew_utilization [default]
├── mart.mart_flight_performance [default]
```

## Building and Running

### Build the Project
```bash
make
```

### Install CLI Globally
```bash
make install
```

### Generate Assets
```bash
teal gen
```

### Run Production Binary
```bash
go run cmd/hello-world/hello-world.go --input-data '{"key":"value"}'
```

### Run Debug UI Binary
```bash
go run cmd/hello-world-ui/hello-world-ui.go
```

The debug UI will be available at `http://localhost:8080`

## Testing

Run tests with:
```bash
go test ./...
```

## Project Structure

```
.
├── assets/
│   ├── models/          # SQL model files organized by stages
│   └── tests/           # SQL test files
├── cmd/
│   ├── hello-world/      # Production binary
│   └── hello-world-ui/   # Debug UI binary
├── internal/
│   ├── assets/          # Generated Go code for models
│   └── model_tests/     # Generated test implementations
├── config.yaml          # Database connections
└── profile.yaml         # Project and model profiles
```

## DAG Visualization

See [graph.wsd](graph.wsd) for the PlantUML diagram of the data pipeline.

---

*This README was automatically generated by Teal*
